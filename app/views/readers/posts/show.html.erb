<% provide(:page_title, @post.title) %>
<div class="row">
  <div class="col-lg-10 offset-lg-1">
    <div class="card post-card mb-5">
      <div class="header-image" style="background-image: url(<%= url_for(@post.header_image) %>)"></div>
      <div class="card-body">
        <div class="pl-3 pr-3 pt-4 pb-4">
          <h1 class="card-title mb-3"><%= @post.title %></h1>
          <div><b>by. Jongwon Lee</b> | <%= @views %> Views (<%= @uniq_views %> Uniq Views) | <%= time_ago_in_words(@post.published_at) %> ago</div>
          <div class="mb-5"><%= @post.hashtag %></div>
          <% @post.elements.each do |element| %>
            <% if element.image? %>
              <div class="image-element mb-2">
                <%= image_tag(element.image) %>
              </div>
            <% else %>
              <div class="content-element mb-2">
                <%= element.content %>
              </div>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>


